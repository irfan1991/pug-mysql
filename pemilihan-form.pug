<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Form Pug
    body
        h2 Demo Pemilihan di dalam Pug
        form(action="/", method="post")
            label(for="bilangan") Masukkan bilangan bulat :
            input(type="number", name="bilangan", size="10")
            input(type="submit", value="Kirim")
<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Pengulangan di dalam Form
    body
        h2 Demo Pengulangan di dalam Form
        form(action="/", method="post")
            div 
                label(for="s") Masukkan teks yang diulang :
                input(type="text", name="s")
            div
                label(for="n") Masukkan jumlah pengulangan :
                input(type="number", name="n", size="10")
            div
                input(type="submit", value="Kirim")
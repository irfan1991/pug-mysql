<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Demo Variable Dalam Pug
    body
        h2 Demo Variable Dalam Pug
        ul(type="none")
            - var i=0
            while i < data.length
                if i % 2 == 1
                    - var s = data[i++].toUpperCase()
                else
                    - var s = data[i++]
                li=s
            